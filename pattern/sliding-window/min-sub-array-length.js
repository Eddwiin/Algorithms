(function () {

    // Exercice by Colt Steele
    // Write a function called  minSubArrayLen which accepts two parameters - an array of positive integers and a positive integer.

    // This function should return the minimal length of a contiguous subarray of which the sum is greater than or equal to the integer passed to the function.
    // If there isn't one, return 0 instead. Examples:

    // Créer une fonction qui va prendre en parametre un tatbleau de int positif et un nombre
    // Parcourir le tableau de int
})()